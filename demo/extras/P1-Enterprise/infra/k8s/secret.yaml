# DO NOT commit real values. Replace placeholders before applying.
# To create: kubectl create secret generic anime-secrets \
#   --from-literal=SECRET_KEY=<your-secret-key> \
#   --from-literal=GROQ_API_KEY=<your-groq-api-key> \
#   --namespace=anime-recommender
apiVersion: v1
kind: Secret
metadata:
  name: anime-secrets
  namespace: anime-recommender
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_WITH_REAL_SECRET_KEY_MIN_32_CHARS"
  GROQ_API_KEY: "REPLACE_WITH_REAL_GROQ_API_KEY"
