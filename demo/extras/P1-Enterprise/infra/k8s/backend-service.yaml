apiVersion: v1
kind: Service
metadata:
  name: anime-backend-svc
  namespace: anime-recommender
  labels:
    app: anime-backend
spec:
  selector:
    app: anime-backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
