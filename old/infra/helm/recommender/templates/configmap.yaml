apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "recommender.name" . }}-config
  labels: {{- include "recommender.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
