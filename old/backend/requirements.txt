# Pinned with compatible-release (~=) ranges; a fully-locked uv/pip-tools lockfile is
# added in Step 22 (CI/CD). Target runtime: Python 3.12.

# --- Web (Step 2: FastAPI + uvicorn, async + SSE) ---
fastapi~=0.115
uvicorn[standard]~=0.34
sse-starlette~=2.1
prometheus-client~=0.21

# --- Config ---
pydantic~=2.10
pydantic-settings~=2.7
python-dotenv~=1.0

# --- Database (D1) ---
sqlalchemy[asyncio]~=2.0
alembic~=1.14
asyncpg~=0.30      # prod async (Postgres)
psycopg[binary]~=3.2  # prod sync driver (SQLChatMessageHistory)
aiosqlite~=0.20    # tests / local

# --- RAG / LLM stack ---
langchain~=0.3.0
langchain-community~=0.3.0
langchain-core~=0.3.0
langchain-groq~=0.2.0
langchain-openai~=0.2.0
langchain-huggingface~=0.1.0
langchain-astradb~=0.5.0

# --- Vector store + advanced retrieval (D2/D6: Qdrant hybrid + rerank) ---
qdrant-client~=1.12
langchain-qdrant~=0.2.0
fastembed~=0.5          # BM25 sparse vectors for hybrid retrieval
flashrank~=0.2          # ONNX cross-encoder reranker (no torch)

# --- Data ---
pandas~=2.2
datasets~=3.2
pypdf~=5.1

# --- Auth (D9) ---
pyjwt~=2.10
cryptography~=44.0   # RS256 (Cognito JWKS) in prod

# --- Observability (D13/D17) ---
opentelemetry-sdk~=1.29
opentelemetry-exporter-otlp-proto-http~=1.29
opentelemetry-instrumentation-fastapi~=0.50b0
langfuse~=2.57

# --- Cache (D10) ---
redis~=5.2

# --- Test ---
pytest~=8.3
httpx~=0.28
fakeredis~=2.26

