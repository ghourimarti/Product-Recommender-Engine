apiVersion: v1
kind: Service
metadata:
  name: qdrant
  labels:
    app: qdrant
spec:
  selector:
    app: qdrant
  ports:
    - protocol: TCP
      port: {{ .Values.qdrant.port }}
      targetPort: {{ .Values.qdrant.port }}
